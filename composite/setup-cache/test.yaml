---
# yaml-language-server: $schema=../../schemas/test.schema.json
version: 1
sources:
  - composite/setup-cache/scripts/cache-plan/main.cjs
tests:
  - id: selection
    description: Select language caches independently of Nix binary caching.
    tools:
      - tact
      - node
    command:
      - tact
      - check
      - cache-plan
      - selection
    expect:
      exit: 0
      calls: []
